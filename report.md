# AskrGomoku 技术报告

我们训练了一个五子棋策略AI模型（15x15棋盘，无禁手）。我们整体上使用了自对弈强化学习作为主要训练方法，卷积神经网络作为主要模型架构。

## 1. 训练侧

### 1.1 自对弈强化学习

众所周知，以ChatGPT-3.5为首的大预言模型主要通过**模仿**真实人类语料进行学习，这被称作***监督学习***（*Supervised Learning*）。

然而，1997年“深蓝”击败人类国际象棋冠军，2016年“AlphaGo”击败人类围棋冠军，这些例子说明在棋类游戏策略中计算机有潜力比人类更优秀，但如果仅通过模仿真实人类对局是永远无法超越人类的。此外，OpenAI已因为使用被版权保护的人类著作而陷入多场诉讼，而高质量的五子棋对局数据库更是难以寻觅。因此，我们没有使用监督学习，而是使用了自对弈强化学习。

***强化学习***（*Reinforcement Learning*）是一种类似“萝卜纸巾猫”的训练方法。我们并不直接指导模型的工作，而是评估模型的成果，并鼓励模型更多输出“好”的结果。即，我们并不要求模型模仿人类的策略$P_{\text{Human}}$，而是让模型任意选择一个策略。如果模型通过策略$P_1$下赢了棋，我们就认为$P_1$的成果是“好”的，鼓励模型更多采用策略$P_1$；如果模型采用策略$P_2$下输了棋，我们就认为$P_2$的成果是“不好”的，就训练模型更少使用策略$P_2$。

***自对弈***是指，我们让模型自己和自己下棋，而不是让模型和人类下棋。这主要是考虑到招募人类对弈的成本，以及人类的对弈水平有限，很难给模型提供有挑战性的对手。

具体来说，所谓***策略模型***（*Policy*），是指以棋盘状态（即当前棋盘上已有的棋子的分布）$S$为输入、落子位置$a$为输出的函数，即$P(S)=a$。如果对于棋盘状态$S_k$，在$a_k$处落子是最佳选项，那么我们就通过修改函数$P$的参数，使得$P(S_k)$趋近于$a_k$。而自对弈是指，模型会从空棋盘$S_{0}$开始，计算黑棋的落点$P(S_{0})=a_0$，由此得到$S_1=S_0+a_0$，接着计算白棋的落点$P(S_1)=a_1$...最终，当$S_n$中某一方出现了五连珠，这一局就结束。如果是黑方胜利，我们就鼓励模型在$S_0$时输出$a_0$。同时，我们也告诉模型，对于盘面$S_1$而言，$a_1$是是一个坏的落点，让模型下次选个别的。反复重复这个过程，模型就可以学会输出好的策略。

#### 1.1.1 奖励稀疏的强化学习

对于五子棋，唯一的***事实基准***（*Ground Truth*）是终局的胜利。

如果某一方取得了胜利，也不能说它下的每一步棋都是好棋：它可能先下了一步有点坏的棋，然后又下了一步绝世好棋把局面救了回来；也可能它先下了好棋把盘面变得极为有利，而后面几手只是平庸地延续了优势。如果我们仅仅根据胜利进行奖励，我们就无法区分哪一步好、哪一步坏，会同时奖励胜利方下的好棋和坏棋。

奖励信号（一局棋的输赢）的数量远少于动作的数量，这种情况被称作***奖励稀疏***（*Reward Sparsity*）。这会导致我们难以区分单个动作的好坏，从而论功行赏。

当然，如果某一方最终获得了胜利，平均而言、概率而言，它下的好棋总是比坏棋更多；而输的一方则会下更多的坏棋。因此，奖励稀疏会显著拖慢强化学习的速度，但通常不会完全阻止学习。如果我们单纯靠大量对弈的概率来估计中盘落子的好坏，这被称作***蒙特卡洛模拟***（*Monte Carlo simulation*）。

为了提升学习速度，我们必须能够评判单个动作的好坏。然而我们并不先验地（a priori）知道在中盘某个动作的好坏。因此，除了训练一个策略模型，我们同时还训练一个***局势***（*value*）模型：这个模型负责评估当前我方有多大的优势。如果当前局势很好，下完这一手后局势变差了，哪怕最终取得了胜利，我们也认为这是一步坏棋，告诉模型不要这样做。如果下完这一手后局势没什么变化，那么我们就认为这是无关紧要的一步棋。

具体来说，局势模型$V$是一个函数，以棋盘状态$S$为输入、局势$v$为输出。$v$是一个取值范围$[-1,1]$的实数，如果$v=1$，意味着在这个棋盘状态我方必胜；$v=-1$意味着在这个棋盘状态我方必败；$v=0$意味着双方处于胶着状态；$v=0.1$意味着我方有微弱的优势。

对于应对棋盘状态$S_k$的落子$a_k$，设$S_k+a_k=S_{k+1}$，那么落子$a_k$的带来优势变化为$v_{k+1}-v_{k}$，或者说$V(S_{k+1})-V(S_{k})$。以$v_{k+1}-v_{k}$作为奖励，这种方法被称作***时序差分***（*Temporal Difference*）。

当然，考虑到局势模型并非全知全能、同样会有误差，我们采取了一个折衷的办法：***广义优势估计***（*Generalized Advantage Estimation*），把时序差分优势和最终盘面的胜负进行加权平均，从而进一步提升论功行赏的准确度。

#### 1.1.2 非传递（循环克制）的自对弈

自对弈，即自己和自己下棋。模型能够学会战胜过去的自己。自对弈训练建立在这样一个假设之上：不断战胜过去的自己，就能变得越来越强。

这个假设在五子棋上十分可疑。我们可以想象，存在棋风各不相同的三个人：A能战胜B，B能战胜C，而C又能战胜A。可能A擅长用斜线攻击，C擅长用直线攻击，而B擅长防守直线、不擅长防守斜线；而C掌握了一个必杀技，这个必杀技恰巧是A的盲点。

这被称为***非传递性***（*non-transitivity*），或者说循环克制。与之相反的则是赛跑：如果A比B跑的快，而B比C跑得快，那么A一定比C跑得快。

如果模型不断和自己对弈，它或许会从风格A出发，逐渐学会风格B；为了战胜风格B的自己，它会学会风格C，接着，为了战胜风格C的自己，它有可能遗忘对付风格B的技巧，又回到风格A。如此往复，实际上模型并不能变得越来越强。

因此，严格来说我们并不采用自对弈。我们为当前的模型构建一个对手池。在这个池中，我们会放入16个各不相同的对手，而这些对手都是过去某个时间点的自己。我们筛选16个对当前的自己最难以战胜的16个过去的自己放入对手池。如果A、B、C同时在对手池中，模型就必须想出来一个同时战胜它们的策略D，从而保证模型的强度一定是在提升的。如果模型更新到N，能够熟练战胜B到M，但忘了如何战胜A，我们就会把A加入对手池，让模型重新学习如何战胜A。

### 1.2 异策略探索

在上一节中，我们阐释了强化学习的基本原理：如果模型采用了一个策略，然后这个策略被发现是好的，就鼓励模型更多采用它。我们没有阐释的是，模型如何可以采用不同的策略？

最自然、被广泛采用的方式是，让策略模型的输出不是一个落子位置（如落在左上方），而是让策略模型同时对所有可能的落子位置打分，再把分数转换为概率输出。我们约定越好的落子位置分数越高。譬如模型可以给左上方打50分，给右上方打40分，左下方打10分。这个表述的意思是，模型认为左上方当然是最优选择，但是右上方也值得一试，而左下方则是不太好的选择。在训练过程中，我们根据这个分数，让模型概率选择不同的落子。比如在10局中有5局让模型选左上，4局让模型选右上，1局让模型选左下。如果这10局中，选左上的5局输了4局、赢了一局，选右上的4局赢了3局、输了1局，那么左上这个位置就得到了4点惩罚和1点奖励，合计是3点惩罚；右上的位置得到3点奖励和1点惩罚，合计2点奖励。这些奖励和惩罚一起，让模型下次会给右上打更高的分数，给左上打更低的分数。这样，模型就完成了对几个不同位置的比较。

上面的方式，使用策略模型自身的打分去探索不同的位置，这种方式叫做***同策略***（*On-Policy*）探索。这种探索的问题是，如果模型一开始错误地给某个位置打了0分，那么这个位置就永远不会被尝试。哪怕这个位置是一个绝好的位置，也永远不会被发现。

当然，同策略探索也有其优势，那就是



#### 1.2.1 择优回溯模仿

#### 1.2.2 熵感知重采样

### 1.3 冷启动奖励填充

## 2. 架构侧

> **备注**：策略头（Policy Head）的中间通道数 `POLICY_HEAD_D` 原始设计为 64（对 `WIDTH=96` 的缩减），但在 `eea7f74`（CLER）提交中被意外提升至 128，变为扩展而非缩减。由于 vanilla 模型已使用 128 完成训练，出于算力限制，我们决定不回退此值，main 模型同样沿用 128。

